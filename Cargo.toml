[package]
name = "printall"
version = "1.0.0"
edition = "2024"
description = "Recursively find PDF and DOCX files and print them"
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive", "env"] }
tempfile = "3.23"
walkdir = "2.5"
